package com.thesett.util.queue;

import java.util.LinkedList;

/**
 * FifoLinkedQeue implements a Queue with FIFO (or in-order) ordering.
 *
 * <pre><p/><table id="crc"><caption>CRC Card</caption>
 * <tr><th> Responsibilities </th><th> Collaborations </th>
 * <tr><td> Add elements to a queue. </td></tr>
 * <tr><td> Take elements from a queue. </td></tr>
 * </table></pre>
 */
public class FifoLinkedQueue<E> extends LinkedList<E> implements Queue<E> {
}
