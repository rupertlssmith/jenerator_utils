package com.thesett.util.entity;

/**
 * EntityNotExistsException indicates that an entity that an operation is being performed on, does not exist, so the
 * operation cannot be performed.
 *
 * <pre><p/><table id="crc"><caption>CRC Card</caption>
 * <tr><th> Responsibilities </th><th> Collaborations </th>
 * <tr><td> Indicates that an entity does not exist, so cannot be operated upon. </td></tr>
 * </table></pre>
 */
public class EntityNotExistsException extends Exception {
}
